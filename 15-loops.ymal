- name: applying conditions
  hosts: localhost
  vars:
  tasks:
  - name: applying loop conditions
    ansible.builtin.debug:
     msg: "hello {{item}}"
    loop:
   - madhu
   - shiva
   - loukya
   - vishruth